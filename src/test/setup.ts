// setup.ts
// Runs before every test file.
// Imports jest-dom matchers so you can write:
//   expect(element).toBeInTheDocument()
//   expect(button).toBeDisabled()
// instead of raw DOM assertions.
import '@testing-library/jest-dom';
